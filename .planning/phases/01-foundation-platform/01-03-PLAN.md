---
phase: 01-foundation-platform
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - examples/01-quickstart.ipynb
  - examples/02-basic-graph.ipynb
  - README.md
  - src/ogma_jupyter/__init__.py
autonomous: false

must_haves:
  truths:
    - "User can run og.demo() and see interactive graph visualization"
    - "User can pan and zoom the visualization with mouse/trackpad"
    - "Same notebook code works in JupyterLab"
    - "Same notebook code works in VSCode notebooks"
    - "Same notebook code works in Google Colab"
    - "Bundled example notebooks show basic usage"
  artifacts:
    - path: "examples/01-quickstart.ipynb"
      provides: "Quick start example notebook"
      contains: "OgmaWidget"
    - path: "examples/02-basic-graph.ipynb"
      provides: "Basic graph creation example"
      contains: "graph_data"
    - path: "README.md"
      provides: "Installation and usage documentation"
      contains: "pip install"
  key_links:
    - from: "examples/*.ipynb"
      to: "ogma_jupyter"
      via: "import statement"
      pattern: "import ogma_jupyter"
    - from: "README.md"
      to: "examples/"
      via: "documentation reference"
      pattern: "examples/"
---

<objective>
Create example notebooks, finalize developer experience, and verify cross-platform
compatibility.

Purpose: Fulfill locked decisions for bundled examples and first-run experience.
Verify the widget works identically across JupyterLab, VSCode, and Google Colab
(Databricks requires actual cluster access).

Output:
- Bundled example notebooks in examples/
- README with installation and usage instructions
- Verified cross-platform compatibility
</objective>

<execution_context>
@/Users/amilevski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amilevski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-platform/01-RESEARCH.md
@.planning/phases/01-foundation-platform/01-01-SUMMARY.md
@.planning/phases/01-foundation-platform/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create example notebooks and README</name>
  <files>
    examples/01-quickstart.ipynb
    examples/02-basic-graph.ipynb
    README.md
    src/ogma_jupyter/__init__.py
  </files>
  <action>
Create example notebooks per locked decision (bundled examples users can copy and run):

**examples/01-quickstart.ipynb:**
- Title: "Ogma Jupyter Quickstart"
- Cell 1 (markdown): Brief intro explaining the package
- Cell 2 (code): `import ogma_jupyter as og`
- Cell 3 (code): `og.demo()` - Shows the demo widget
- Cell 4 (markdown): Explanation of what's displayed
- Cell 5 (code): Creating custom graph:
  ```python
  widget = og.OgmaWidget()
  widget.graph_data = {
      'nodes': [{'id': 'a'}, {'id': 'b'}, {'id': 'c'}],
      'edges': [{'source': 'a', 'target': 'b'}, {'source': 'b', 'target': 'c'}]
  }
  widget
  ```
- Cell 6 (markdown): Next steps and link to 02-basic-graph.ipynb

**examples/02-basic-graph.ipynb:**
- Title: "Basic Graph Creation"
- Demonstrates:
  - Creating nodes with data attributes
  - Creating edges with data attributes
  - Updating graph_data after widget creation
  - Multiple widgets in same notebook
- Include clear markdown explanations between code cells

**README.md:**
- Badge placeholders (PyPI version, Python version, License)
- One-line description
- Installation: `pip install ogma-jupyter`
- Ogma license setup section:
  - Set OGMA_LICENSE_KEY environment variable
  - Or pass license_key to constructor
  - Link to Linkurious for obtaining license
- npm registry configuration (if needed for Ogma)
- Quick start code example
- Link to examples/ directory
- Development setup instructions
- License section

**Update src/ogma_jupyter/__init__.py:**
- Add get_example_path() function that returns Path to examples/ directory
- This allows users to find bundled examples: `og.get_example_path()`
- Docstring explains how to copy examples to their workspace
  </action>
  <verify>
    - examples/01-quickstart.ipynb is valid JSON: python -c "import json; json.load(open('examples/01-quickstart.ipynb'))"
    - examples/02-basic-graph.ipynb is valid JSON: python -c "import json; json.load(open('examples/02-basic-graph.ipynb'))"
    - README.md exists and has installation section: grep "pip install" README.md
    - get_example_path function exists: python -c "from ogma_jupyter import get_example_path; print(get_example_path())"
  </verify>
  <done>
    - examples/01-quickstart.ipynb demonstrates og.demo() and basic widget
    - examples/02-basic-graph.ipynb shows detailed graph creation
    - README.md documents installation, license setup, and usage
    - get_example_path() function added to package
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cross-platform compatibility</name>
  <what-built>
    Complete ogma-jupyter package with widget, examples, and documentation.
    The widget should render interactive Ogma graph visualizations with pan/zoom.
  </what-built>
  <how-to-verify>
**Test in JupyterLab (PRIMARY):**
1. Start JupyterLab: `jupyter lab`
2. Open examples/01-quickstart.ipynb
3. Run all cells
4. Verify: Graph visualization appears with 3 nodes
5. Verify: Can pan (click and drag) and zoom (scroll wheel)
6. Verify: Welcome message appeared on first import (if first run)

**Test in VSCode:**
1. Open the project in VSCode
2. Open examples/01-quickstart.ipynb
3. Select Python kernel
4. Run all cells
5. Verify: Same visualization appears and is interactive

**Test in Google Colab:**
1. Upload examples/01-quickstart.ipynb to Google Colab
2. Add cell at top: `!pip install ogma-jupyter` (or install from local wheel)
3. Run all cells
4. Verify: Visualization renders correctly

**Expected behavior in all platforms:**
- 3-node triangle graph visible
- Pan: click and drag moves the view
- Zoom: scroll wheel or pinch zooms in/out
- Nodes are clickable (may not have handler yet, but should respond)

**Known limitations to document:**
- Databricks testing requires actual cluster (defer to later if no access)
- Colab may have slight rendering differences
- If Ogma license not configured, widget should show helpful error
  </how-to-verify>
  <resume-signal>
Type "approved" if widget works in JupyterLab + at least one other platform.
If issues found, describe: platform, what failed, error messages if any.
  </resume-signal>
</task>

</tasks>

<verification>
1. Example notebooks valid and runnable:
   - python -c "import json; json.load(open('examples/01-quickstart.ipynb'))"
   - jupyter execute examples/01-quickstart.ipynb (if headless execution supported)

2. README complete:
   - grep -q "pip install" README.md
   - grep -q "OGMA_LICENSE_KEY" README.md
   - grep -q "examples" README.md

3. Human verification of visual functionality:
   - Widget renders in notebook cell
   - Pan and zoom work
   - Cross-platform compatibility confirmed
</verification>

<success_criteria>
- examples/01-quickstart.ipynb demonstrates basic usage
- examples/02-basic-graph.ipynb shows detailed graph creation
- README.md provides complete installation and usage guide
- Widget renders interactive visualization in JupyterLab
- Widget renders in at least one additional platform (VSCode or Colab)
- Pan and zoom interaction works
- Human has verified visual functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform/01-03-SUMMARY.md`
</output>
