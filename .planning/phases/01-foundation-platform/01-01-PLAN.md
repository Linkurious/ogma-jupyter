---
phase: 01-foundation-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - package.json
  - tsconfig.json
  - src/ogma_jupyter/__init__.py
  - src/ogma_jupyter/widget.py
  - src/ogma_jupyter/config.py
  - src/ogma_jupyter/errors.py
  - src/ogma_jupyter/_version.py
autonomous: true

must_haves:
  truths:
    - "Python package structure exists with src layout"
    - "pyproject.toml has hatchling build backend configured"
    - "package.json has esbuild and Ogma dependencies"
    - "OgmaWidget class extends anywidget.AnyWidget"
    - "License key loads from OGMA_LICENSE_KEY env var or constructor"
    - "Custom exceptions provide helpful error messages with fix suggestions"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata and build configuration"
      contains: "hatchling"
    - path: "package.json"
      provides: "JavaScript dependencies and build scripts"
      contains: "esbuild"
    - path: "src/ogma_jupyter/__init__.py"
      provides: "Public API exports"
      exports: ["OgmaWidget", "demo", "__version__"]
    - path: "src/ogma_jupyter/widget.py"
      provides: "Main widget class"
      contains: "class OgmaWidget"
    - path: "src/ogma_jupyter/config.py"
      provides: "Configuration and license key management"
      contains: "get_license_key"
    - path: "src/ogma_jupyter/errors.py"
      provides: "Custom exceptions with helpful messages"
      contains: "class OgmaError"
  key_links:
    - from: "pyproject.toml"
      to: "hatch-jupyter-builder"
      via: "build-system requires"
      pattern: "hatch-jupyter-builder"
    - from: "src/ogma_jupyter/widget.py"
      to: "anywidget.AnyWidget"
      via: "class inheritance"
      pattern: "class OgmaWidget.*anywidget\\.AnyWidget"
    - from: "src/ogma_jupyter/__init__.py"
      to: "src/ogma_jupyter/widget.py"
      via: "import"
      pattern: "from \\.widget import OgmaWidget"
---

<objective>
Create the ogma-jupyter package scaffold with Python core components.

Purpose: Establish the project foundation with modern Python packaging (hatchling),
JavaScript build configuration (esbuild), and the core Python widget class using
anywidget framework.

Output:
- Complete pyproject.toml with hatch-jupyter-builder hooks
- package.json with esbuild build scripts
- Python package with OgmaWidget, config, and error handling
</objective>

<execution_context>
@/Users/amilevski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amilevski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-platform/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold with build configuration</name>
  <files>
    pyproject.toml
    package.json
    tsconfig.json
    .gitignore
  </files>
  <action>
Create the project foundation files:

**pyproject.toml:**
- name: "ogma-jupyter"
- requires-python: ">=3.10"
- dependencies: ["anywidget>=0.9.21"]
- build-system requires: ["hatchling>=1.28", "hatch-jupyter-builder>=0.9"]
- build-backend: "hatchling.build"
- Add [tool.hatch.build.hooks.jupyter-builder] section:
  - dependencies: ["hatch-jupyter-builder"]
  - build-function: "hatch_jupyter_builder.npm_builder"
  - ensured-targets: ["src/ogma_jupyter/static/widget.js"]
- Add [tool.hatch.build.hooks.jupyter-builder.build-kwargs]:
  - npm: "npm"
  - build_cmd: "build"
- Add dev dependencies: pytest>=8.0, ruff>=0.4, jupyterlab>=4.0
- Add project metadata: description, authors, license, classifiers

**package.json:**
- devDependencies: @anywidget/types ^0.2.0, esbuild ^0.24.0, typescript ^5.4
- scripts.build: "esbuild --bundle --format=esm --outdir=src/ogma_jupyter/static js/src/index.ts"
- scripts.dev: same with --watch flag
- Note: Ogma dependency will require user's npm registry config (document in README)

**tsconfig.json:**
- target: ES2020
- module: ESNext
- moduleResolution: bundler
- strict: true
- esModuleInterop: true

**.gitignore:**
- Add: src/ogma_jupyter/static/ (compiled JS)
- Add: node_modules/
- Add: dist/, build/, *.egg-info
- Add: .pytest_cache/, __pycache__/
- Add: .ipynb_checkpoints/

Create directory structure:
- src/ogma_jupyter/
- src/ogma_jupyter/static/ (empty, will hold compiled JS)
- js/src/
- examples/
- tests/
  </action>
  <verify>
    - pyproject.toml exists and is valid TOML (python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))")
    - package.json exists and is valid JSON (node -e "require('./package.json')")
    - Directory structure: ls src/ogma_jupyter js/src examples tests
  </verify>
  <done>
    - pyproject.toml configured with hatchling and hatch-jupyter-builder
    - package.json configured with esbuild build scripts
    - tsconfig.json ready for TypeScript compilation
    - All required directories created
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Python package core with widget, config, and errors</name>
  <files>
    src/ogma_jupyter/__init__.py
    src/ogma_jupyter/_version.py
    src/ogma_jupyter/widget.py
    src/ogma_jupyter/config.py
    src/ogma_jupyter/errors.py
  </files>
  <action>
Create the Python package core following patterns from RESEARCH.md:

**src/ogma_jupyter/_version.py:**
- Single source of truth: __version__ = "0.1.0"

**src/ogma_jupyter/errors.py:**
- OgmaError(Exception): Base exception
- OgmaLicenseError(OgmaError): Missing/invalid license key with detailed message
- OgmaDataError(OgmaError): Invalid data format with fix suggestions
- Per locked decision: Error messages must include actionable fix suggestions
- Example: "Expected nodes as list of dicts, got DataFrame. Use from_dataframe() instead."

**src/ogma_jupyter/config.py:**
- get_license_key(provided_key: Optional[str] = None) -> str
  - Priority: 1) provided_key, 2) OGMA_LICENSE_KEY env var, 3) raise OgmaLicenseError
  - Error message includes both env var and constructor approaches
- _WELCOME_SHOWN_FILE = Path.home() / '.ogma_jupyter_welcome'
- _maybe_show_welcome(): Shows "Welcome to Ogma Jupyter! Run og.demo() to see an example."
  - Only on first run (checks _WELCOME_SHOWN_FILE)
  - Creates marker file after showing
  - Fails silently if can't write marker

**src/ogma_jupyter/widget.py:**
- class OgmaWidget(anywidget.AnyWidget):
  - _esm = pathlib.Path(__file__).parent / "static" / "widget.js"
  - graph_data = traitlets.Dict({'nodes': [], 'edges': []}).tag(sync=True)
  - _license_key = traitlets.Unicode('').tag(sync=True)
  - __init__(self, graph_data=None, license_key=None, **kwargs)
  - Comprehensive docstring with Parameters, Returns, Examples sections
  - Per locked decision: docstrings show in IDE autocomplete

**src/ogma_jupyter/__init__.py:**
- Import and export: __version__, OgmaWidget, OgmaError, OgmaLicenseError, OgmaDataError
- Call _maybe_show_welcome() at import time
- def demo() -> OgmaWidget: Returns widget with sample 3-node triangle graph
- Module docstring with usage examples
- __all__ list for explicit exports
  </action>
  <verify>
    - Python syntax valid: python -m py_compile src/ogma_jupyter/*.py
    - Imports work: python -c "from ogma_jupyter.errors import OgmaError, OgmaLicenseError"
    - Config works: python -c "from ogma_jupyter.config import get_license_key"
    - Widget defined: python -c "from ogma_jupyter.widget import OgmaWidget; print(OgmaWidget)"
  </verify>
  <done>
    - OgmaWidget class extends anywidget.AnyWidget with graph_data trait
    - License key loads from env var with constructor fallback
    - Custom exceptions provide helpful messages with fix suggestions
    - Welcome message shows on first import
    - demo() function returns sample widget
    - All docstrings include params, returns, examples
  </done>
</task>

</tasks>

<verification>
1. Project structure complete:
   - ls -la src/ogma_jupyter/ shows __init__.py, widget.py, config.py, errors.py, _version.py
   - ls -la js/src/ directory exists (empty for now)

2. Build files valid:
   - python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))"
   - node -e "require('./package.json')"

3. Python imports work:
   - python -c "import ogma_jupyter; print(ogma_jupyter.__version__)"
   - python -c "from ogma_jupyter import OgmaWidget, demo"

4. Error messages are helpful:
   - python -c "from ogma_jupyter.errors import OgmaLicenseError; print(str(OgmaLicenseError('test')))"
</verification>

<success_criteria>
- pyproject.toml exists with hatchling + hatch-jupyter-builder configuration
- package.json exists with esbuild build scripts
- src/ogma_jupyter/ contains all Python modules
- OgmaWidget class can be imported (will fail at runtime until JS is built)
- Error classes provide actionable fix suggestions in their messages
- Welcome message logic implemented (may not show if marker exists)
- demo() function defined and returns OgmaWidget
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform/01-01-SUMMARY.md`
</output>
